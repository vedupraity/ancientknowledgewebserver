name: Build and Deploy
on:
  push:
    branches:
      - feature/github-actions
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.8'
          architecture: 'x64'
      - name: 📥 Install
        run: |
          echo "using python $(python3 --version)"
          echo "using pip $(pip3 --version)"
          # pip3 install -r requirements.txt
      - name: ⚒️ Build
        run: |
          # uncomment below line to freeze
          # source .env.prod && python freeze.py
          mkdir build
          echo "hellow" > build/index.html
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out my other private repo
        uses: actions/checkout@master
        with:
          repository: vedupraity/ancientknowledgewebsite
          token: ${{ secrets.PAT_GITHUB_REPO }}
      - name: Cache build artifacts
        id: build-artifact
        uses: actions/cache@v2
        with:
          key: .
          path: ./build
      - name: 🚀 Deploy
        run: |
          echo "🚀 Deploying to production"
          ls -lah
          cat index.html
      
      #     git clone https://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@github.com/vedupraity/ancientknowledgewebsite.git
      #     ls -lah
      #     cd ancientknowledgewebsite
      #     git config user.name "Ved (bot)"
      #     git config user.email "vedprakashupraity@gmail.com"
      #     ls -lah

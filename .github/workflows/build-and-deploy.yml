name: Build and Deploy
on:
  push:
    branches:
      - feature/github-actions
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v2
        with:
          python-version: '3.8'
          architecture: 'x64'
      - run: echo "📥 Installing dependencies"
      - run: |
        python3 --version
      - run: echo "⚒️ Building static pages"
      - run: echo "🚀 Deploying to production"
